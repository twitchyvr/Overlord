Build me prompt of a complete and runnable codebase to create a tiny GPT-style language model (around 10-30M parameters) from scratch using PyTorch trained on a small plain English md dataset, designed to run locally on a single consumer GPU (I have RTX 5070) without distributed training, including tokenizer training (SentencePiece), a simple causal Transformer architecture (self-attention, LayerNorm, MLP, positional embeddings), next-token pretraining, a lightweight supervised fine-tuning phase for basic chat formatting, optional simple JSON output support, clear model configuration and hyperparameters, train/validation splitting, mixed precision and gradient accumulation setup, overfitting mitigation for small data, and a minimal CLI script for local chat inference, all organized in a clean folder structure with execution steps and concise explanations, optimized for clarity, educational value, and successful completion over a weekend with minimal dependencies.